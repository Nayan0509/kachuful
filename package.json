{
  "name": "kachuful",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client && node scripts/copy-build.js",
    "start": "node server/index.js"
  }
}
